{% extends 'base.html.twig' %}

{% block title %}Hello SearchController!{% endblock %}

{% block body %}
<div class="row">



    {% for annonce in annonces %}
    <div class="col border border-primary m-3 p-3">
        {{annonce.title}} <br>
        <img height="200" width="200" src="{{annonce.coverImage}}" alt=""> <br>
        {{annonce.introduction}} <br>
        Auteur: {{annonce.author.firstname}} <br>
        {{annonce.content}} <br>
        {% include 'shared/rating.html.twig' with {'rating': annonce.AvgRating} %} <br>

        {% if annonce.comments|length > 0 %} <br>

        {% else %}

        <small>pas encore noté</small> <br>
        {% endif %}

        <span class-"m-3"> <a class=" btn btn-primary btn-md" href="{{path('annonce', {'slug' : annonce.slug})}}">Voir
                page
                détail</a>
        </span>


    </div>

    {% endfor %}
</div>
{% endblock %}
